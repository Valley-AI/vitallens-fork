name: Tests

on:
  push:
    branches: [dev, main, 'test/*']
  pull_request:
    branches: [main]

jobs:
  linux:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: ["20", "22"]
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v6
        with:
          node-version: ${{ matrix.node-version }}

      - name: Install ffmpeg
        uses: Iamshankhadeep/setup-ffmpeg@v1.2
        with:
          version: "4.4"
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Install dependencies
        run: npm ci

      - name: Security audit
        run: npm audit --audit-level=high

      - name: Set development VITALLENS_FILE_ENDPOINT for non-main branches
        if: github.ref != 'refs/heads/main'
        run: echo "VITALLENS_FILE_ENDPOINT=https://api-dev.rouast.com/vitallens-dev/file" >> $GITHUB_ENV
      
      - name: Set development VITALLENS_STREAM_ENDPOINT for non-main branches
        if: github.ref != 'refs/heads/main'
        run: echo "VITALLENS_STREAM_ENDPOINT=https://api-dev.rouast.com/vitallens-dev/stream" >> $GITHUB_ENV

      - name: Lint code
        run: npm run lint

      - name: Run tests
        env:
          VITALLENS_DEV_API_KEY: ${{ secrets.VITALLENS_DEV_API_KEY }}
        run: npm test

  windows:
    runs-on: windows-latest
    strategy:
      matrix:
        node-version: ["20", "22"]
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v6
        with:
          node-version: ${{ matrix.node-version }}

      - name: Install ffmpeg
        uses: Iamshankhadeep/setup-ffmpeg@v1.2
        with:
          version: "4.4"
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Install dependencies
        run: npm ci

      - name: Security audit
        run: npm audit --audit-level=high

      - name: Set development VITALLENS_FILE_ENDPOINT for non-main branches
        if: github.ref != 'refs/heads/main'
        run: echo "VITALLENS_FILE_ENDPOINT=https://api-dev.rouast.com/vitallens-dev/file" | Out-File -FilePath $env:GITHUB_ENV -Append

      - name: Set development VITALLENS_STREAM_ENDPOINT for non-main branches
        if: github.ref != 'refs/heads/main'
        run: echo "VITALLENS_STREAM_ENDPOINT=https://api-dev.rouast.com/vitallens-dev/stream" | Out-File -FilePath $env:GITHUB_ENV -Append

      - name: Lint code
        run: npm run lint

      - name: Run tests
        env:
          VITALLENS_DEV_API_KEY: ${{ secrets.VITALLENS_DEV_API_KEY }}
        run: npm test
